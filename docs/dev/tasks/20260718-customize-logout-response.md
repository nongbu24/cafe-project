# 로그아웃 성공 응답 정리

## 목적

- 로그아웃 성공 응답의 메시지를 `로그아웃이 완료되었습니다.`로 변경한다.
- 성공 응답의 `data`가 `null`이면 JSON 응답에서 제외한다.

## 변경

- `ApiResponse`의 `data` 필드에 null 제외 직렬화 설정을 추가한다.
- 로그아웃 컨트롤러에서 전용 성공 메시지를 반환한다.
- 로그아웃 API 문서와 컨트롤러 테스트를 갱신한다.

## 검증

- `./gradlew test --tests 'com.example.cafe.auth.*'`: 성공
