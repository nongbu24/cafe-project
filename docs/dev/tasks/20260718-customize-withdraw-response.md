# 회원탈퇴 성공 응답 메시지 정리

## 목적

- 회원탈퇴 성공 응답의 메시지를 `회원 탈퇴가 완료되었습니다.`로 변경한다.
- 반환 데이터가 없는 응답에서 `data` 필드가 포함되지 않는 계약을 테스트와 문서에 반영한다.

## 변경

- 회원탈퇴 컨트롤러에서 전용 성공 메시지를 반환한다.
- 회원탈퇴 컨트롤러 테스트와 인증 API 문서를 갱신한다.

## 검증

- `./gradlew test --tests 'com.example.cafe.auth.*'`: 성공
