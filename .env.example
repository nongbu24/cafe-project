# JWT 서명 키: openssl rand -base64 32 명령으로 생성한다.
JWT_SECRET=

# Redis 연결 정보
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# PostgreSQL 연결 정보
POSTGRES_HOST=localhost
POSTGRES_DB=cafe
POSTGRES_USER=cafe
POSTGRES_PASSWORD=
POSTGRES_PORT=5432

# PostgreSQL로 애플리케이션을 실행할 때만 사용한다.
# SPRING_PROFILES_ACTIVE=postgres
